from modelizations.basic_modelization import *
from modelizations.abstract_modelization import *


def parse_problem(path: str) -> Problem:
    return #TODO


def generate_problem(file_name: str) -> None:
    '''Add parameters to generate different types of problem easily'''
    with open('data_sample/' + file_name + '.txt', 'x') as file:
        file.write()  #TODO
